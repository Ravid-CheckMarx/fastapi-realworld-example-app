requisits:
 - Create an admin user with admin/password
 - The player will have to find (via risk 2) and login to this user
 - Add "admin" field to register, login, login with auth response with the value of "null"
 - User update his info with "admin":"true" or login with admin creds, we need to update the db accordignly

Plan:
 - The user will update his info and make him admin (admin == true)
 - We need to check whether the username is not admin and admin == true (regular user that became an admin)
 - If so, return the flag in the response for update user
